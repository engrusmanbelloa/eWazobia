import { StyleSheet, Text, View } from "react-native"
import React from "react"

export default function forgortPassword() {
  return (
    <View>
      <Text>forgortPassword</Text>
    </View>
  )
}

const styles = StyleSheet.create({})
