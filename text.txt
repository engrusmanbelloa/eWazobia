// Import the necessary components and screens from the "onboarding" folder
import { OnboardingScreen1, OnboardingScreen2, OnboardingScreen3 } from "../onboarding"

// ... existing code ...

export default function TabLayout() {
  const colorScheme = useColorScheme()

  // Check if the desired behavior is to show screens without tabs
  const showOnboardingScreens = true // Set this to true to show onboarding screens without tabs

  if (showOnboardingScreens) {
    // Render the onboarding screens without tabs
    return (
      <>
        <OnboardingScreen1 />
        <OnboardingScreen2 />
        <OnboardingScreen3 />
      </>
    )
  } else {
    // Render the tabs with the configured screens
    return (
      <Tabs
        screenOptions={{
          tabBarActiveTintColor: Colors[colorScheme ?? "light"].tint,
        }}
      >
        {/* ... existing tab screens ... */}
      </Tabs>
    )
  }
}


export default function OnboardingLayout() {
  return (
    <Stack>
      <Stack.Screen
        name="onboardingScreen1"
        component={OnboardingScreen1}
        options={{ headerShown: false }}
      />
      <Stack.Screen
        name="onboardingScreen2"
        component={OnboardingScreen2}
        options={{ headerShown: false }}
      />
      <Stack.Screen
        name="onboardingScreen3"
        component={OnboardingScreen3}
        options={{ headerShown: false }}
      />
    </Stack>
  );
}


 <Link href="/home">Go to Home</Link>

      <Button
        title="Go to Details... again"
        onPress={() => router.push("/details")}
      />
      <Button title="Go back" onPress={() => router.back()} />
      <Button
        title="Go back to first screen in stack"
        onPress={() => navigation.popToTop()}
      />